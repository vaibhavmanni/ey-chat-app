version: '3.8'
services:
  db:
    image: postgres:15
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: chatapp
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: chatapp_dev
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
